{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install -g pnpm && export PATH=$(npm root -g)/.bin:$PATH && cd skill_swap_app && pnpm install --no-frozen-lockfile && pnpm --filter @skillswap/api build"
  },
  "deploy": {
    "startCommand": "node /app/skill_swap_app/apps/api/dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
