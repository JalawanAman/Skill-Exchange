// Database schema — Drizzle ORM
// Full table definitions mirror 04_DATABASE_SCHEMA.md exactly
// Tables are added per milestone — schema grows with the build

// M1 tables: users, credit_transactions
// M2 tables: skills, skill_offers, skill_wants, availability
// M3 tables: matches, blocks
// M4 tables: connection_requests, conversations
// M5 tables: messages
// M6-7 tables: sessions, credit_transactions (extended)
// M8 tables: reviews, notifications, reports

export {}
// Schema definitions added starting M1 — see 04_DATABASE_SCHEMA.md
